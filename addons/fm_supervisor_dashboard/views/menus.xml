<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_fm_supervisor_dashboard" model="ir.actions.client">
        <field name="name">Supervisor Board</field>
        <field name="tag">fm_supervisor_dashboard</field>
    </record>

    <!-- Non-default menu item so a JS issue can never block the FM app. -->
    <menuitem id="menu_fm_supervisor_dashboard"
              name="Supervisor Board"
              parent="fm_asset.menu_fm_root"
              action="action_fm_supervisor_dashboard"
              sequence="3"
              groups="fm_branding.group_fm_dispatcher"/>

</odoo>
